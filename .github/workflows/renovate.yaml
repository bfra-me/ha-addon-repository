---
# Renovate this repository if this workflow file or the Renovate configuration file is changed.
name: Renovate

'on':
  pull_request:
    branches:
      - main
    paths:
      - '.github/workflows/renovate.yaml'
      - '.github/renovate.json'
  push:
    branches:
      - main
    paths:
      - '.github/workflows/renovate.yaml'
      - '.github/renovate.json'

permissions: {}

jobs:
  Renovate:
    uses: bfra-me/renovate-config/.github/workflows/renovate.yaml@main
    with:
      autodiscover: false
      branch: ${{ github.event_name == 'pull_request' && github.head_ref || github.event.repository.default_branch }}
      dry_run: ${{ github.event_name == 'pull_request' && true }}
      print_config: true
    secrets: inherit
