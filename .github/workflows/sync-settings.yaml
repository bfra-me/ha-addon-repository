name: Sync Settings

on:
  push:
    branches: [main]
    paths:
      - '.github/settings.yml'
      - '.github/workflows/sync-settings.yaml'
  schedule:
    - cron: '15 14 * * *'
  workflow_dispatch:

permissions: {}

jobs:
  sync-settings:
    runs-on: ubuntu-latest
    steps:
      - name: Get Token
        id: get_workflow_token
        uses: peter-murray/workflow-application-token-action@8e1ba3bf1619726336414f1014e37f17fbadf1db # v2.1.0
        with:
          application_id: ${{ secrets.APPLICATION_ID }}
          application_private_key: ${{ secrets.APPLICATION_PRIVATE_KEY }}
          organization: ${{ github.repository_owner }}
          permissions: 'administration:write, contents:read, issues:write'

      - name: Sync Settings to GitHub
        uses: exivity/actions/sync-defaults@70b72b7b5245f3fea77ec260a3a10176ecf37164 # main
        with:
          gh-token: ${{ steps.get_workflow_token.outputs.token }}
